---
description: Build reusable, accessible, production-ready UI components — every state covered, nothing hardcoded.
argument-hint: [component to build]
---
Act as a senior frontend engineer building reusable, accessible, production-ready
UI components.

Build: $ARGUMENTS

Cover: loading, empty, and error states; edge cases; responsive layout; and
accessibility (keyboard navigation, ARIA, focus management).

Deliver: the component architecture, props / API design, and the full
implementation. Keep it composable — no one-off hardcoding. Match the project's
existing component patterns and styling approach.
